\documentclass[prd, twocolumn, superscriptaddress, nofootinbib,
floatfix]{revtex4-2}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage[normalem]{ulem}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

\DeclareRobustCommand{\Eq}[1]{Eq.~\eqref{eq:#1}}
\DeclareRobustCommand{\fig}[1]{Fig.~\ref{fig:#1}}
\DeclareRobustCommand{\figs}[2]{Figs.~\ref{fig:#1} and \ref{fig:#2}}
\DeclareRobustCommand{\app}[1]{App.~\ref{app:#1}}
\DeclareRobustCommand{\sect}[1]{Sec.~\ref{sec:#1}}
\DeclareRobustCommand{\tb}[1]{Table~\ref{tab:#1}}
\DeclareRobustCommand{\tbs}[2]{Tables~\ref{tab:#1} and~\ref{tab:#2}}

\newcommand{\JH}[1]{{\color{blue}JH: {#1}}} %% comments from Jinchen

\graphicspath{{figures/}}

% REVTeX turns on \flushbottom in twocolumn mode, which forces both
% columns to end on the same baseline by stretching the glue between
% paragraphs. On a sparse column that shows up as visibly uneven gaps around
% individual paragraphs. Let the columns end where they end instead.
\raggedbottom

\begin{document}

\title{Diffusion models as preconditioners for hybrid Monte Carlo in
lattice gauge theory}

\author{Shivam Panda}
% \affiliation{}
\author{Jinchen He}
% \affiliation{}
\author{Yong Zhao}
% \affiliation{}

\begin{abstract}
Hybrid Monte Carlo (HMC) sampling of lattice gauge fields suffers from critical
slowing down, and its topological charge can freeze entirely at small lattice
spacing. We show that a diffusion model used as a preconditioner can bypass
topological freezing: the chain is handed a starting configuration whose
topology is \JH{\sout{already} approximately} correct, \JH{\sout{so it is never asked to sample a quantity it
cannot move} so it can thermalize in a short time}. The diffusion model
performs an inverse renormalization-group \JH{\sout{step} transform?}, \JH{\sout{lifting} upsampling} a coarse configuration
to a fine one, and its output preconditions the HMC chain in place of a cold or
hot start. The topological charge is transported from the coarse configuration
rather than generated \JH{from scratch}, so it \JH{\sout{is correct by construction} only needs small corrections}. In 2D U(1) and U(2), the
method delivers configurations at $\beta \approx 1660$ on a $128\times128$
lattice, eight times the linear size of the $16\times16$ \JH{lattice on which it was trained.}
\JH{\sout{Transport is exact at every step, so the charge is sampled once at $\beta = 28$,
where an exact winding update is not frozen, and carried unchanged to a
coupling where neither plain nor winding-update HMC equilibrates.}}
\JH{(* maybe make the following three sentences shorter by summarize the optimization to a factor of xxx? *)}After $400$ trajectories the delivered ensemble matches the exact closed form
to $1$ part per million on the plaquette and $340$ on an $8\times8$ Wilson
loop, against $51$ and $7600$ parts per million for a cold start over the same
budget, whose topological charge never moves. Measured in trajectories per
independent configuration, the preconditioned chain is unboundedly cheaper than
plain HMC above $\beta \approx 25$, where HMC supplies no independent
configurations at any budget. Against HMC with an exact winding update, the
strongest classical sampler this theory admits, it is roughly break-even below
$\beta \approx 90$ and unbounded above it, where that baseline stops
equilibrating. Widening the trained range from $\beta \approx 416$ to $\beta \approx 2000$
carries the method from failing above its old ceiling to working at every
coupling tested, out to $\beta \approx 1660$, and per-configuration accuracy
does not degrade at four times the volume.
\end{abstract}

\maketitle

\section{Introduction}
\label{sec:intro}
Lattice Quantum Chromodynamics (QCD) \JH{\sout{discretizes the strong interaction
non-perturbatively.} provides a non-perturbative description of the strong interaction in a discretized setup of space-time. }
\JH{\sout{Configurations, 4D grids carrying an SU(3) matrix on
every edge, together with a dynamical fermion field, are sampled from the
Boltzmann distribution $p(\phi)\propto e^{-S[\phi]}$ in the lattice action $S$,
and observables are Monte Carlo estimates over that ensemble.}
In lattice QCD calculations, the path integral is replaced by sample averaging over measurements on Euclidean configurations, which are usually generated from Markov chain Monte carlo according to the Boltzmann distribution $p(\phi)\propto e^{-S[\phi]}$ with a given lattice action $S$. }

The cost of Hybrid Monte Carlo (HMC) grows close to exponentially as the lattice
spacing shrinks, a phenomenon known as critical slowing down
\cite{schaefer2011}; and generation separately suffers from topological
freezing, where large action barriers leave
charge sectors unexplored \cite{luscher2011}, so a batch of configurations no
longer follows the expected distribution of topological charge
(\sect{freezing}).

An HMC chain is conventionally initialized from a cold or a hot configuration.
A cold start sets every link to the identity; a hot start draws each link
independently from the Haar measure on the gauge group. Neither carries any
information about the target, so a chain begun from either must run for a
thermalization time $t_{\rm therm}$ before its samples are usable. We show that
$t_{\rm therm}$ can be cut from hundreds of trajectories to a few, or to zero,
by using a diffusion model as a preconditioner instead. The model inverts one
renormalization-group (RG) step: it takes a configuration on an $L\times L$
lattice at coupling $\beta$ and generates one on $2L\times2L$ at roughly
$4\beta$, conditioned on the coarse configuration it came from. Iterating that
climb reaches lattices and couplings where direct sampling has stalled, while
the charge is not generated at any step but carried up from the base exactly.

Full QCD being expensive, we work in 2D U(1) and 2D U(2), which keep the
features that matter and are exactly solvable, so every output can be scored
against a closed form rather than against another simulation. U(1) is abelian,
one phase per link; U(2) is a non-abelian step beyond it and carries topological
charge the same way, since $U(2)=(U(1)\times SU(2))/Z_2$ and SU(2) is
topologically trivial in 2D, so the machinery built for U(1) transfers.

Machine learning has been applied to this problem largely through normalizing
flows \cite{albergo2019} and diffusion models: Wang, Aarts and Zhou cast the
reverse diffusion as a stochastic-quantization scheme for lattice field theory
\cite{wang2024}, Zhu et al.\ applied one to 2D U(1) configurations
\cite{zhu2024,zhu2025}, conditioning on the coupling so that one model covers a
range of them, and Aarts et al.\ generalized to non-abelian 2D U(2)/SU(2) with
an equivariant architecture \cite{aarts2026}. In each of these the learned model
is the sampler: the ensemble it produces is the result, and correctness is
either taken from the model directly or restored by an accept/reject step
wrapped around it, as in the Metropolis-adjusted annealed Langevin sampler of
\cite{aarts2026}. Either way the burden falls on the model at the target
volume, and \cite{zhu2025,aarts2026} both report that it transfers to larger
couplings and lattice sizes. A separate line of work
starts HMC from a constructed configuration instead: Detmold and Endres
\cite{detmoldendres2015,detmoldendres2016} built a renormalization-group-matched
coarse-to-fine map, started HMC from its output, and measured rethermalization
against cold and hot starts, for pure Yang--Mills and then with fermions.

This paper is organized as follows: \sect{setup} fixes notation and defines the classical baselines, \sect{method}
gives the method, \sect{seedquality} and \sect{coverage} report what the
preconditioner is worth and what training must cover, and \sect{conclusions}
asks what carries over to full QCD.

\section{Setup: 2D U(1) and U(2)}
\label{sec:setup}

\subsection{Links, plaquettes, action and topological charge}

The degrees of freedom are gauge links $U_{x,\mu}$ on the edges of a periodic lattice.
We follow the conventions of \cite{gattringerlang2010}
throughout. In 2D U(1) a link is a phase $U_{x,\mu}=e^{i\theta_{x,\mu}}$; in 2D
U(2) it is a unitary $2\times2$ matrix, which for computational convenience we
store in the split representation
\begin{equation}
    U_{x,\mu} = e^{i\theta_{x,\mu}}\begin{pmatrix}
q_1 + i q_4 & q_3 + i q_2 \\
-q_3 + i q_2 & q_1 - i q_4
\end{pmatrix},
    \label{eq:u2-link}
\end{equation}
an SU(2) element written as a unit quaternion ($\sum_a q_a^2 = 1$) times a
central phase, so five real scalars describe a U(2) link.

Every observable is built from the plaquette
\begin{equation}
    U_{x,p} \equiv U_{x,\mu} U_{x+\hat{\mu},\nu} U_{x+\hat{\nu},\mu}^\dagger U_{x,\nu}^\dagger,
    \label{eq:plaquette}
\end{equation}
the smallest closed loop and hence gauge invariant; products of adjacent
plaquettes form the larger Wilson loops used as observables. In U(1) the
plaquette reduces to a signed sum of link angles,
\begin{equation}
    \theta_{x,p} \equiv \theta_{x,\mu} + \theta_{x+\hat{\mu},\nu} - \theta_{x+\hat{\nu},\mu} - \theta_{x,\nu},
    \label{eq:theta-plaquette}
\end{equation}
whose order is immaterial; in U(2) the matrix ordering matters. The Wilson
actions,
\begin{align}
    S_{U(1)} &= -\beta \sum_{x}\cos(\theta_{x,p}), \label{eq:action-u1}\\
    S_{U(2)} &= -\beta \sum_{x} \tfrac{1}{2}\mathrm{Re}\,\mathrm{Tr}(U_{x,p}),
    \label{eq:action-u2}
\end{align}
are gauge invariant since they depend only on plaquettes.

The topological charge is the wrapped plaquette sum,
\begin{equation}
    Q = \frac{1}{2\pi}\sum_{x} \mathrm{wrap}\,(\theta_{x,p}),
    \label{eq:q-u1}
\end{equation}
where $\mathrm{wrap}(\alpha) \equiv \mathrm{atan2}(\sin\alpha,\cos\alpha)$
returns the representative in $(-\pi,\pi]$, the two-argument arctangent being
what resolves the quadrant.

For U(2), $Q$ is defined by the same formula applied to the determinant plaquette
phase, $\alpha_x = \mathrm{wrap}(\arg\det U_{x,p})$. The central phases in
\Eq{plaquette} are scalars, so they factor out of the product and combine into
exactly the U(1) plaquette sum of \Eq{theta-plaquette}; the SU(2) factors have
unit determinant and drop out of it entirely. Hence
\begin{equation}
    \det(U_{x,p})=e^{2i\theta_{x,p}}.
    \label{eq:det-plaquette}
\end{equation}
The SU(2) component therefore contributes nothing to $Q$, as it cannot: SU(2)
is simply connected, so it carries no topological charge in two dimensions. We call the U(1) component of a U(2) link
its determinant sector, $\psi \equiv \arg\det U = \mathrm{wrap}(2\theta)$.
Note the factor of two: $Q$ counts the winding of $\det U$, and by
\Eq{det-plaquette} the determinant phase advances twice as fast as the
central phase $\theta$ does. A configuration whose links are purely central
therefore carries twice the charge the same phases would carry in U(1), which is
why the U(1) instanton moves $Q$ by two rather than by one when it is
transferred to U(2) (\sect{instanton_updates_classical}).

\subsection{Topological freezing, and HMC}
\label{sec:freezing}

Since $Q$ is defined by the wrapped sum of plaquettes, it can only change if some
$\theta_{x,p}$ is pushed past $\pm\pi$. At large $\beta$ every plaquette angle
clusters near zero due to the Wilson actions in \Eq{action-u1}.
Changing $Q$ through purely local updates therefore requires passing through an
intermediate configuration with some $\theta_{x,p}$ near $\pm\pi$, which costs
$\Delta S = 2\beta$ and is accepted with probability $\sim e^{-2\beta}$. That exponential
suppression is the root cause of topological freezing, and it worsens toward the
continuum limit as $\beta$ grows. By \Eq{det-plaquette} the mechanism
is identical in U(2).

Configurations are generated by HMC \cite{duane1987,neal2011}, discretized by
the Omelyan integrator \cite{omelyan2003} for U(1) and by an exponential-map
leapfrog for the group-valued U(2) fields. The whole trajectory is offered as a
single Metropolis proposal, which is what guarantees sampling from the Boltzmann
distribution despite the integrator's discretization error.

\subsection{The classical baseline: winding updates}
\label{sec:instanton_updates_classical}

In 2D U(1), winding HMC \cite{albandea2021} proposes an instanton field, a
field carrying exactly one unit of topological charge, as a global update
accepted by the same Metropolis criterion. It delays the freezing barrier to
much higher $\beta$ than standard HMC without eliminating it
(\tb{u1_frozen}), and being the stronger comparison it is the
classical baseline throughout this paper.

\label{sec:instanton}%
In 2D U(2) an analogous construction is available because $Q$ depends only on
$\psi$. The SU(2) sector integrates out of the action exactly, plaquette by
plaquette against Haar measure, which is a 2D solvability statement with no
analogue in $D>2$ or with fermions. That integral gives the closed-form marginal
weight $w_{\det}(\alpha;\beta)=2I_1(z)/z$, $z=\beta\cos(\alpha/2)$
(\sect{u2-extension}).

The U(1) instanton transfers to U(2), but only halfway. A purely central phase shift
$\theta\to\theta+\lambda$ commutes with the SU(2) sector entirely, so it can be
applied without disturbing it, at the same cost $\Delta S = 2\pi^2\beta/V$ as in
U(1).
But the determinant sector is $\psi=2\theta$, so the shift moves $\psi$ by
$2\lambda$ and $Q$ by exactly two. Every proposal changes $Q$ by two, so it
cannot change the parity of $Q$, meaning whether $Q$ is even or odd: a chain
under this move stays for its whole history in the parity class it started in.
We call it the even-only move, and a move that crosses between the two classes a
parity flip.

Reaching an odd sector from an even one takes three steps: (i) propose
$\psi'=\psi\pm\lambda$, changing $Q$ by $\pm1$; (ii) accept with the Metropolis
ratio of the marginal action alone, $\min(1,e^{-\Delta S_{\det}})$, leaving the
SU(2) sector out of the decision because it is about to be replaced; and (iii)
resample $q\sim p(q\mid\psi')$ exactly by Creutz--Kennedy--Pendleton heatbath
\cite{creutz1980,kennedypendleton1985}. We call this the marginal move.

At $L=64$ and $\beta=416.524$, $200$ cold-started trajectories
over $64$ chains give zero accepted parity flips under the even-only move, so the
measured odd-sector weight is $P(\mathrm{odd})=0$ against an exact $0.4929$.
Under the marginal move the same budget gives $7661$ flips and
$P(\mathrm{odd})=0.4939$. \tb{classical} repeats the
comparison at $L=16$ across three couplings.

\section{Method}
\label{sec:method}

\subsection{Blocking, the matched ladder, and sector coverage}
\label{sec:naive-blocking}

Where the baselines of \sect{setup} repair topology inside a running chain, the
method here acts once, before the chain starts. It is one inverse
renormalization-group step: a coarse configuration at
$L_c\times L_c$ and coupling $\beta_c$ is lifted to a fine one at $L_f=2L_c$ and
$\beta_f\approx4\beta_c$, with the fine topological charge transported exactly
from the coarse configuration and only the remaining ultraviolet structure
supplied by the model. We call one such $(L,\beta)$ point a \emph{rung}, and
the sequence of rungs running from the base ensemble up to the target the
\emph{ladder}.

A fine configuration is generated at a $\beta$ small enough that HMC with winding updates has a finite decorrelation
time even for the slow modes. Its coarse partner is fixed completely by summing the neighboring links,
\begin{align*}
\theta_x'(X,Y) &= \mathrm{wrap}\big(\theta_x(2X,2Y) + \theta_x(2X+1,2Y)\big),\\
\theta_y'(X,Y) &= \mathrm{wrap}\big(\theta_y(2X,2Y) + \theta_y(2X,2Y+1)\big),
\end{align*}
with $\theta'$ the coarse link angle. This makes the coarse plaquette angle equal
the wrapped sum of its four constituent fine sub-plaquettes exactly, and hence
$Q_{\mathrm{coarse}} = Q_{\mathrm{fine}}$.

The two couplings are related through the first-order character coefficient
$r_1(\beta)=I_1(\beta)/I_0(\beta)$, the leading term in the Fourier expansion of
the single-plaquette Wilson weight. Matching it is the minimum
Kullback--Leibler projection onto the Wilson family, which for a one-parameter
exponential family is moment matching on its sufficient statistic
\cite{amarinagaoka2000}. A coarse plaquette is built from four fine ones, so blocking
maps the coefficient as $r_1(\beta_c) = r_1(\beta_f)^{4}$, which follows from
the exact solvability of 2D Wilson theory
\cite{migdal1975,drouffezuber1983} and reduces to the tree-level
$\beta_c\approx\beta_f/4$ through $I_1/I_0\approx 1-1/(2\beta)$.

How wide the charge distribution $P(Q)$ is, and hence how many sectors an
ensemble populates,
is measured by $\chi_t V$, the topological susceptibility times the volume,
which is $\langle Q^2\rangle$ itself \cite{bonatirossi2019}. To leading order
$\chi_t V \approx V/(4\pi^2\beta_1)$, and this is held fixed by the inverse RG step since doubling $L$ quadruples $V$ while the matched coupling
quadruples $\beta_1$. That near-invariance is a property of the two-dimensional
definition of $Q$ rather than of how the ladder is matched, and it is why a
plaquette-matched ladder preserves $\langle Q^2\rangle$ approximately.

At high $\beta$ the fine ensemble used to build training pairs is itself frozen
in $Q$, with $\langle Q^2\rangle\ll1$, so HMC returns configurations almost
entirely at $Q=0$. A training set built purely from HMC output at a fixed high
coupling would then never show the network the local structure of a
configuration with nonzero $Q$ at that coupling. The network needs exactly that
structure, because at deployment a coarse input can carry any $Q$ and the
transport step will impose it regardless.

We manufacture the missing coverage rather than sampling it, by two mechanisms
that we distinguish because only one of them needs the theory to be solvable.
Sector augmentation takes a fixed fraction of the fine ensemble at a rung, draws
a charge shift uniformly from a small integer range, adds that many copies of the
instanton field (\sect{instanton}), and relaxes the result locally at fixed
topology to remove the ultraviolet error the instanton leaves behind. It
consults no closed form. The alternative is to draw a configuration's charge
from the exact $P(Q)$ and impose it, which does.

\subsection{Score network}
\label{sec:scoring-network}

\begin{figure*}[bt]
  \centering
  \includegraphics[width=0.9\linewidth]{u1_2d/45_architecture.png}
    \caption{The gauge-covariant score network, shown for U(1), with
  $324{,}954$ parameters in $4$ FiLM blocks of width $56$. The U(2) network has
  the same structure, with width $64$ and $403{,}394$ parameters. The network
  outputs one number per plaquette and turns these into a score with a lattice
  curl, which can represent the gradient of any sum of functions of plaquettes,
  so this form loses no generality. No part of the network depends on the
  lattice size $L$, because the convolutions are circular and each site is
  normalized on its own. One trained network can therefore be used at every
  lattice size on the ladder.}
  \label{fig:architecture}
\end{figure*}

Diffusion models generate by reversing a noising process. A forward It\^o SDE
$dx = f(x,t)dt + g(t)dW$ carries the data distribution to a tractable reference
over $t\in[0,T]$. By Anderson's theorem \cite{anderson1982} it has a
reverse-time companion with the same marginals, whose only unknown is the score
$\nabla_x\log p_t(x)$; denoising score matching \cite{vincent2011} estimates
that score by regressing on the known conditional transition kernel, and Song et
al.\ \cite{song2021} supply the discretizations used here. The standard
construction assumes $x\in\mathbb R^n$ with a Gaussian kernel. Neither holds for
link angles, which live on a compact periodic domain, so both the forward
process and the score it induces are built from the domain's own kernel.

Each configuration is a vector $\vec\theta$ with $2L^2$ components, noised
independently with the wrapped Gaussian transition kernel
$$K(\theta;\hat\theta,\sigma_t)=\frac{1}{\sigma_t\sqrt{2\pi}} \sum_{r=-\infty}^{\infty} \exp\!\left(-\frac{(\theta-\hat{\theta} + 2\pi r)^2}{2\sigma_t^2}\right),$$
with $\hat\theta$ the clean angle. As $\sigma_t$ grows this approaches the uniform
distribution on the circle, and the regression target is $\nabla_\theta\log K$.

The architecture is shown in \fig{architecture}. The network sees two
fields, the noisy fine configuration and the coarse configuration conditioning
it, each as gauge-invariant channels. Because the two live on grids of different
size, the coarse field is first enlarged to the fine grid by nearest-neighbour
replication, each coarse value copied onto all four sites of its block, after
which the two are concatenated channel-wise. The noise level $\sigma_t$ and
coupling $\beta$ enter through a sinusoidal embedding feeding per-block
Feature-wise Linear Modulation (FiLM) \cite{perez2018}, into which the
spatial mean of the coarse conditioning channels is also projected. That gives
every block direct access to a genuinely global quantity, since the mean wrapped
coarse plaquette angle equals $2\pi Q_{\mathrm{coarse}}/V$ by definition
of $Q$, and a convolutional network with bounded kernel size cannot otherwise
reconstruct a lattice-wide sum.

Gauge invariance is built into the architecture, by the construction given
below. The Wilson action's $90^\circ$-rotation, reflection and
charge-conjugation symmetries are not. Training instead applies a uniformly
random element of that 16-element group to each fine configuration before
blocking and noising.

The network predicts one scalar $h_p$ per plaquette rather than a link-valued
score, and the score is assembled by the lattice curl,
\begin{align}
  s_x(x,y) &= h_p(x,y) - h_p(x,y-1), \label{eq:curl-x}\\
  s_y(x,y) &= h_p(x-1,y) - h_p(x,y). \label{eq:curl-y}
\end{align}
This is the general form of the gradient of any sum of functions of plaquettes.
Each link belongs to exactly two plaquettes with opposite orientation, so
$\partial\theta_p/\partial\theta_\ell\in\{+1,-1,0\}$ and only two terms survive.
Because $h_p$ is itself a function of gauge-invariant inputs, the score is gauge-covariant by construction.

Network capacity, the training budget and the couplings each checkpoint was
trained on, together with the sampler and rethermalization settings used
throughout, are collected in \app{settings}.

\subsection{Reverse diffusion, and exact charge transport}
\label{sec:reverse_diffusion}

\begin{figure*}[bt]
    \centering
    \includegraphics[width=\linewidth]{reverse_diffusion_schematic.png}
    \caption{The reverse-diffusion chain for 2D U(1) and U(2)
    (\sect{reverse_diffusion}). It runs from right to left, starting from angles
    $\phi_T$ drawn uniformly on the circle and ending at the lifted configuration
    $\phi_0$. Here $s_\theta$ is the learned score and
    $\Delta_t=\sigma_t^2-\sigma_{t-1}^2$, and each denoising step is followed by
    one Langevin correction step (dashed). Before it is handed to HMC, the
    configuration is rethermalized with exact local updates, preceded in U(2) by
    the conditional SU(2) heatbath of \sect{u2-extension}.}
    \label{fig:method_schematic}
\end{figure*}

The wrapped-Gaussian kernel is exactly the transition kernel of a driftless
diffusion wrapped onto the circle, so $f\equiv0$ and
$g(t)=\sqrt{d\sigma_t^2/dt}$: a variance-exploding schedule in the terminology of
\cite{song2021}. The forward process is $d\theta = g(t)dW$ and its reverse-time
companion is
$$d\theta = -g(t)^2\,s_\theta(\theta_t,\sigma_t,\beta)\,dt + g(t)\,d\bar W,$$
so the entire drift of the reverse process is the learned score itself. Starting from pure noise on the fine lattice, ancestral sampling integrates this
backward through the noise schedule (\fig{method_schematic}). Every result reported here takes one
Langevin corrector step after each ancestral step \cite{song2021}.

Three mechanisms softly bias $Q_{\mathrm{fine}}$ toward $Q_{\mathrm{coarse}}$
during sampling: the coarse-conditioning channels, the FiLM-embedded global
mean, and a reconstruction-guidance penalty added to the score at each noise
level. That penalty is the gradient, with respect to the cell sum, of
$$-\frac{\big(\sum_{p=1}^4\theta_{p,\mathrm{fine}} - \theta_{p,\mathrm{coarse}}\big)^2}
        {2\lambda(\sigma)}, \qquad \lambda(\sigma)\approx 8\sigma^2,$$
in which $\lambda(\sigma)$ is the noise that the eight boundary links of a coarse
cell inject into the blocked plaquette at level $\sigma$. The per-cell scalar is broadcast to the four fine sub-plaquettes and
assembled into link space by the same discrete curl used for the score head, so
the guidance is gauge-covariant by construction. None of these make $Q_{\mathrm{fine}}=Q_{\mathrm{coarse}}$ exact, they only
make the sampler more likely to settle in the correct sector on its own.

Exactness comes instead from a fourth step: the sampler computes
$\Delta Q = Q_{\mathrm{coarse}} - Q_{\mathrm{fine}}$ and adds $\Delta Q$ copies
of the instanton field of \sect{instanton}. It does this once every ten
reverse-SDE steps below $\sigma\approx0.5$, the level at which $Q$ becomes a
well-defined quantity of the noisy sample, and once more unconditionally after
sampling completes. Applying the projection partway through the reverse
process, rather than only at the end, lets the remaining diffusion steps and the
rethermalization tail relax the ultraviolet error it leaves behind; the final
unconditional application fixes the delivered configuration's topology however
close the soft mechanisms got on their own.

The rethermalization tail separates the two things we score. The raw lift is the
reverse diffusion output before it, and the delivered ensemble is what it
returns. Both are reported wherever they differ, because cheap local sweeps
repair ultraviolet error but cannot touch topology, so the accuracy demanded of
the model is not the same at the two scales.

\subsection{Extension to U(2)}
\label{sec:u2-extension}
\label{sec:instanton_apply}

The construction carries over to U(2) essentially unchanged. The lifting step is a
conditional diffusion model that generates only the determinant sector $\psi$,
with $\psi$ standing in for $\theta$ throughout. Integrating SU(2) out of one
plaquette against Haar measure is the standard one-link integral
\cite{creutz1980}, and gives the exact marginal weight of the determinant-sector
plaquette angle $\alpha$,
$$w_{\det}(\alpha;\beta) = \frac{2I_1(z)}{z},
\qquad z=\beta\cos(\alpha/2).$$
This is not of Wilson form, so the U(1) coupling $\beta_1$ that the network is
conditioned on is fixed by the same minimum-KL projection used for the ladder in
\sect{naive-blocking}, applied here to $w_{\det}$. The Wilson single-plaquette
weight is a von Mises distribution in $\alpha$ with concentration $\beta_1$ and
sufficient statistic $\cos\alpha$, so the projection reads
$$\frac{I_1(\beta_1)}{I_0(\beta_1)} = \langle\cos\alpha\rangle_{w_{\det}},$$
the standard concentration estimate for that family \cite{mardiajupp2000}.

Two couplings therefore appear throughout. The \emph{raw} coupling is
$\beta_{U(2)}$ itself, the coefficient in the U(2) Wilson action of
\Eq{action-u2}; the \emph{model} coupling is $\beta_1$, the matched
U(1) value the score network is conditioned on. Training coverage is stated in
model $\beta$, while ensembles, actions and every coupling quoted in the results
are raw $\beta$ unless said otherwise.

The SU(2) sector needs no model. It cannot be generated on its own, because the
U(2) action does not factorize into independent U(1) and SU(2) parts, so $q$
must be drawn conditioned on $\psi$. At fixed $\psi$ that conditional is the
standard SU(2) single-link weight,
$p(q \mid \psi) \propto \exp(\beta\, k \cdot q)$. Here $k$ is the U(2) staple
sum, rotated by the link's own phase and written as a quaternion, and that
rotation carries the whole dependence on $\psi$. A fresh SU(2) sector can therefore be equilibrated exactly by
Creutz--Kennedy--Pendleton heatbath sweeps
\cite{creutz1980,kennedypendleton1985}, which leave $\psi$, and hence $Q$,
unchanged.

\section{Preconditioner quality}
\label{sec:seedquality}

With the map and the transport step in place, what remains is to measure what
the configuration they produce is worth as a starting point. Every comparison
here is scored against exact results, which matters at large $\beta$, where a
reference ensemble built from HMC would itself be frozen.
Wilson loops from the plaquette up to $8\times8$, probing both ultraviolet and
infrared structure, together with $P(Q)$ and the topological susceptibility $\chi_t$, all
follow from the single-plaquette character coefficients of each theory.
\tbs{u1-exact-observables}{u2-exact-observables} in \app{exact} collect them. Local observables and topology are
scored separately, each in its own unit. A Wilson loop is quoted as its relative
deviation from exact, $(\text{mean}-\text{exact})/\text{exact}$, in parts per
million (ppm). A charge variance is quoted as a ratio instead,
$\langle Q^2\rangle$ over its exact value, because it is a variance rather than
a mean and spans zero to many times exact.

\label{sec:relaxation-correction}
The interval $t_{\rm interval}$ is the cost of one independent configuration in
a Markov chain, $2\tau_{\rm int}$ with $\tau_{\rm int}$ the integrated
autocorrelation time estimated by Sokal's automatic windowing
\cite{madrassokal1988,wolff2004}, maximised over the scored observables
(\app{stats}) and over $Q^2$, so that the cost of an independent
\emph{configuration} is set by the slowest mode rather than by any one
observable. A series that does not move at all is reported as infinite rather
than integrated. \sect{cost} takes up what these conventions are for.

The equilibration time $t_{\rm therm}$ follows Detmold and Endres
\cite{detmoldendres2015,detmoldendres2016}: the first trajectory after which the
bias against the exact value stays within two standard errors for five
consecutive measurements, maximised over the Wilson loops but not over $Q^2$: a
frozen chain has an exactly constant $Q^2$, which would make every classical
equilibration time infinite and the comparison empty. Topology is scored
separately, against the closed form.

A classical chain pays $t_{\rm therm}$ once and then an interval for every configuration after the
first, so over an ensemble the interval is what
remains. Successive preconditioned chains descend from independent coarse
configurations and owe no interval, but each pays its own $t_{\rm therm}$. The
ratio of the two is the improvement factor,
\begin{equation}
    F = \frac{t_{\rm interval}\ (\text{classical})}
             {t_{\rm therm}\ (\text{preconditioned})},
    \label{eq:improvement-factor}
\end{equation}
and it is what we report throughout.

\subsection{U(1)}
\label{sec:u1obs}

The model is meant to produce a preconditioner rather than a final
configuration, so all four arms are scored after thermalization rather than on
their raw output. An arm is one run of the benchmark, differing from the others
only in its starting configuration and in which winding update is enabled, and all
four (the preconditioned chain, and cold, hot and winding-update classical
starts) are advanced by the same sampler for the same number of trajectories.

The preconditioned configuration sits inside the exact plaquette's error band already at $t=0$, before
any trajectory, at every coupling tested ($\beta_f = 4.44$ to $218.58$, a
$50\times$ range). The classical baseline of record (cold start, HMC with the
winding update) reaches the same band within a one-time burn-in of $500$
trajectories at the two easier couplings ($\beta_f=4.44$ and $14.15$), but not
at the two largest. At $\beta_f=55.02$ it fails the quality criterion after
$500$ trajectories and needs $8000$ to pass, and at $\beta_f=218.58$ it fails at
$500$ and is still outside the band after $8000$ (\tb{u1_h2h}). The
classical entry cost therefore rises by at least an order of magnitude across
this range, while the lift's is zero throughout.

The accuracy survives a change of volume. At fixed $\beta_f=14.1464$ over
$L=32$, $64$ and $128$, the preconditioned plaquette sits $869$, $892$
and $820$ parts per million from exact before any trajectory. That flatness is
what the architecture predicts: the network is fully convolutional and
conditioned on the coupling rather than on the lattice size, so nothing in it
ties the map to the volumes it trained on. Topology is the exception, and we
report it: $\langle Q^2\rangle$ comes out $0.95\pm0.10$ and $1.20\pm0.18$ times
exact at $L=32$ and $64$, but $0.71\pm0.13$ at $L=128$. The
cold arm does no better there: its charge never moves at any of the three
volumes, so its $\langle Q^2\rangle$ vanishes identically.

The advantage in equilibration cost does not survive equally. Over the same
three volumes the preconditioned chain needs $9$, $18$ and $52$ trajectories to
equilibrate against a cold start's $168$, $400$ and $528$, a factor of $19$,
$22$ and $10$. Both arms are scored on all of their chains, which is the
conservative comparison here: the preconditioned arm carries the larger number,
so it is held to the tighter band.

\subsection{U(2)}
\label{sec:u2obs}
For 2D U(2) at $L=64$, $\beta=416.5$, \tb{u2_observables} gives every arm's
mean Wilson loop after $400$ trajectories on $64$ chains, averaged over the
settled half of each history, together with $\langle Q^2\rangle$ against an
exact $1.0012$. All three arms started
from a preconditioned configuration land within $1.5$~ppm of exact on the
plaquette, $11$~ppm on $W(2\times2)$ and $1300$~ppm on $W(8\times8)$, and carry
$\langle Q^2\rangle$ between $1.00$ and $1.14$ times exact. No classical arm
reaches both the local and the topological target.

A cold start under the same sampler and the same budget is $51$, $229$ and
$7644$~ppm high on those three loops, six to sixty times further from exact than
the preconditioned arms on the plaquette and $W(2\times2)$, and its topological charge
never moves in the $400$ trajectories, so its $\langle Q^2\rangle$ is exactly
zero times exact rather than merely small. Adding a winding update repairs only the charge:
$\langle Q^2\rangle$ comes back to $0.89$ of exact under the even-only move and
$0.98$ under the marginal move, while the plaquette is unchanged at $57$ and
$56$~ppm. A hot start does not begin to equilibrate within the
budget: it is $63{,}000$~ppm low on the plaquette and $990{,}000$~ppm low on
$W(8\times8)$, which is to say that loop is consistent with zero, and it carries
$\langle Q^2\rangle$ at $88$ times exact. Full tables are in \app{tables}.

One variable governs where the preconditioner degrades: how far the test
coupling sits from the couplings the checkpoint was trained on.
\sect{coverage} measures it.

\subsection{Distribution comparison}
\label{sec:distributions}

The comparisons above are all of means, and a preconditioner must do more than
reproduce a mean: the
configurations it delivers are used individually, so their spread has to be the
theory's too. \fig{uv_distributions} and \fig{topology_distributions} in \app{tables} compare
the per-configuration distributions against the closed form, in both theories.

Distances here are quoted in units of $\sigma_{\rm cfg}$, the spread of a given
Wilson loop across individual configurations of the preconditioned ensemble.
On loops from the plaquette out to $W(8\times8)$ the
preconditioned distribution is centred on exact to within
$0.05\,\sigma_{\rm cfg}$ at every loop size, and its width matches that of an
independently equilibrated cold chain to within $3\%$, so the spread is the
theory's rather than an artefact of the model. The hot arm matches
neither: it is displaced by $8$ to $1200\,\sigma_{\rm cfg}$, and its width is
wrong by up to a factor of $25$ in both directions. At the largest loop it is
too narrow rather than too wide, because that loop has collapsed to a value
consistent with zero. On $P(Q)$ the preconditioned histogram tracks
the exact sector weights, while a cold start puts every configuration at $Q=0$
and a hot start scatters more than half its weight beyond the plotted range at
$L=64$.

\subsection{Cost accounting}
\label{sec:cost}
The $t_{\rm interval}$ measurement counts only where the chain has
equilibrated. HMC carries its own accept/reject step, so the distribution it
samples is the right one and a long enough chain is correct for every
observable. What fails within a finite budget is ergodicity: at these couplings the chain
does not change $Q$ once, so it explores only the distribution conditioned on
the single sector it started in.
Local observables are unbiased inside that sector and decorrelate quickly there,
which is why a plaquette interval looks short on precisely the arms that are
worst off; the ensemble average is nonetheless wrong, by the amount the sector
weight is wrong. A short interval measured on such a chain is not a supply of
independent configurations of the target, so where a classical arm does not
equilibrate within the budget we take its cost as infinite and report $F$ as a
lower bound. What $F$ counts on both sides is HMC trajectories; the cost of
building the preconditioned configuration in the first place falls outside that
count and is treated separately in \app{overhead}.

Two classical baselines are separated because they behave differently.
Plain HMC is the generic sampler, and by $\beta\approx25$ its topology no longer
moves. HMC augmented
with the winding update of \sect{instanton_updates_classical} is the strongest
classical sampler, and it holds much further.

\fig{money} measures the improvement factor against the winding baseline across
the coupling range, at both evaluated volumes, and the answer has two regimes.

Below $\beta\approx90$ the winding baseline equilibrates at twelve of the
couplings tested, and there the factor is measured rather than bounded. It is
modest: a median of $1.1$, ranging from $0.1$ to $12$, and below one at five of
the twelve. Counted in trajectories alone and against the strongest classical
sampler this theory admits, the preconditioner is break-even at best, which is
the expected result in a regime where a good classical sampler already exists.

Above $\beta\approx90$ no classical arm reaches equilibrium within $400$
trajectories at any coupling tested, so by the gate of \sect{cost} its cost is
infinite and the factor is a lower bound, running from $1.4$ to $400$ across
the couplings evaluated with a median of $14$. What is measured there is failure to
equilibrate within the budget, not a frozen chain: the winding arm's interval
stays short, between $4$ and $45$ trajectories, and it is the bias rather than
the decorrelation that never settles. Only for plain HMC, whose topology does
not move at all, is the cost infinite at any budget. For the winding arm it is
not topology that fails: the move keeps firing, and even at $\beta=1660$ it
holds $\langle Q^2\rangle$ between $0.79$ and $1.25$ times exact
(\tb{u2_L128}); it is the local observables that never settle.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{u2_2d/fig65_ladder.png}
  \caption{Cost of one independent, correctly distributed configuration along
  the ladder. Each step doubles $L$ and quadruples $\beta$, which keeps
  $\chi_t V$ nearly constant, so the three rungs describe the same physical
  system at three lattice spacings. The first two rungs use the deployed
  checkpoint and the third uses the wider-coverage one, since only its training
  range reaches $\beta=1660$. (a) The number of trajectories the preconditioned
  chain needs to equilibrate, and the number the classical chains need between
  independent configurations. The preconditioned chain is already equilibrated
  at the first two rungs (dotted line). At the first rung the classical chains
  give an independent configuration every $5$ trajectories, after $260$ to
  $270$ trajectories of burn-in. (b) The resulting improvement factor. Every
  value is a lower bound: arrows mark rungs where no classical chain
  equilibrated within the $400$-trajectory budget, and at the first rung the
  preconditioned cost of zero is counted as one trajectory.}
  \label{fig:ladder}
\end{figure*}

\subsection{Reach in volume}
\label{sec:multi-lift}

Reaching a target $(L,\beta)$ may take several lifts, and the error does not
compound when it does (\app{multilift}): it enters at the last rung, so
endpoint accuracy is set by that rung's distance from training coverage and not
by the number of rungs climbed. Laddering therefore buys volume, not coupling
reach, since each lift multiplies $\beta$ by about four and the last one lands
at the same fine coupling whatever path reached it.

The ladder was pushed one rung past the deployed configuration, to $L=128$ at
$\beta=1660.08$, four times the largest linear size in the training set and
sixteen times the sites (\fig{ladder}). Before a single trajectory the
preconditioned configuration sits $0.8$~ppm from the exact plaquette against a
cold start's $1200$~ppm, and it reaches equilibrium in $25$--$30$ trajectories. Neither classical arm reaches equilibrium at all within
the budget, so the improvement factor there is a lower bound.

\section{Training coverage}
\label{sec:coverage}

Where the preconditioner degrades is governed by coupling coverage: which
couplings a checkpoint was trained on, and how far the test coupling sits from
them. This section measures how far that reach extends, and what widening the
training range recovers.

A checkpoint's \emph{training ceiling} is the largest coupling at which it was
ever given training data, so a test coupling above it is an extrapolation and
one below it an interpolation between rungs it has seen. It is a property of the
training set, not of performance. Because the network is conditioned on the fine
side of each training pair, the ceiling is the largest coupling a checkpoint was
asked to generate \emph{at}, with the coarse side near $\beta_f/4$; ceilings are
quoted in raw $\beta$ throughout, matching \fig{money}.

A checkpoint can be trained at a coupling where the classical sampler does not
deliver, because an ensemble is built once and can afford a burn-in no deployed
chain could (\app{settings}). That settles local structure but not topology, so
the charge content is manufactured by the two mechanisms of
\sect{naive-blocking}, of which only exact-$P(Q)$ seeding consults a closed
form. Wrong sector frequencies cost nothing downstream, since the network is
asked only for local structure at a given charge and the charge itself is
transported at deployment.

The experiment in this section is retraining with various training rungs, referred to as training settings. Within each 
training setting, the checkpoints share
architecture, capacity and data recipe and differ only in which fixed coupling
rungs their training sets contain, so any change is attributable to coverage
alone. Four U(2) checkpoints are compared: \texttt{cov60} and \texttt{default}
are narrow, with ceilings at $\beta=227$ and $416$, \texttt{default} being the
one deployed for every other result here; \texttt{wide} extends the ceiling to
$\beta=2000$ using $23$ rungs against \texttt{default}'s $11$; and
\texttt{wide\_dense} adds $31$ further rungs inside that range without extending
it. The U(1) set is built the same way, with ceilings $\beta=60$ and $2000$.
Each is scored over a grid held fixed across checkpoints and run twice, once
with plain HMC in every arm and once with the winding update, so preconditioned
and classical starts are always paired under an identical sampler.
\fig{money} draws \texttt{default} and \texttt{wide\_dense}; all four are
tabulated in \app{tables} with the matching U(1) retrain.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{u2_2d/fig64_money.png}
  \caption{Improvement factor over HMC with the winding update, as a function of
  the coupling at both lattice sizes, for the deployed checkpoint and for one
  trained over a wider range of couplings. Circles are measured values.
  Triangles are lower bounds, at couplings where no classical chain equilibrated
  within $400$ trajectories. Crosses mark couplings where the lift itself did
  not equilibrate. Dotted lines mark the training ceilings. The two measured
  values below one at $L=32$, near $\beta\approx25$ and $\beta\approx89$, are
  couplings where the winding baseline is still cheap, giving an independent
  configuration every $8$ to $14$ trajectories, while the deployed lift needs
  $62$ and $80$ trajectories to equilibrate. The wider checkpoint dips at the
  same couplings because all of its extra training couplings lie above
  $\beta=416$, so below that it was trained on the same couplings.}
  \label{fig:money}
\end{figure*}

Range is the axis that moves the coupling at which the method stops working.
\texttt{default} stops equilibrating
above $\beta=415$ at $L=32$ and above $\beta=94$ at $L=64$, while
\texttt{wide\_dense} works at every coupling tested, to $\beta=1623$ and
$\beta=1660$ respectively. \texttt{wide} does not: it fails at $L=64$ at
$\beta=174$, $637$ and $1660$, all inside its own trained range, so range alone
does not guarantee that the coupling reached moves with it. Density acts elsewhere, and much more weakly. Adding $31$ rungs inside
\texttt{wide}'s existing range improves it at $30$ of $44$ couplings, but by a
median of $2.1\times$ where range gives one to two orders of magnitude, and the
gain is not resolved anywhere: the interval spans unity both inside coverage and
past the ceiling. In U(1) density changes nothing measurable at all.
\tb{u2_coverage_tests} carries both comparisons, with the caveats that qualify
their size. The two axes therefore act where the other does not, and on the
evidence here a fixed rung budget is better spent extending the range than
resampling what it already covers.

In \fig{money}, the curves are jagged because $t_{\rm therm}$ is a discrete first-crossing
index, quantized by the spacing of the measurements and movable by a single
lagging chain out of $64$; adjacent couplings can therefore differ by an order
of magnitude, and the level of a curve carries more than any point on it. Above
$\beta\approx90$ every point is also a lower bound, since no classical arm
equilibrates there and the plotted value is the budget divided by the
preconditioned cost alone: the peak at $L=64$, $\beta=174$ is one of these,
where the lift arrives already equilibrated against $t_{\rm therm}$ of $44$ and
$84$ trajectories at its neighbours. That there is no clean trend in $\beta$ is itself the
result: the factor tracks distance to the nearest training rung, and the rungs
sit at fixed, unevenly spaced couplings, so that distance falls and rises again
as $\beta$ passes each in turn.

Two limits bound what this grid can show. The first is geometric. The grid holds
$L$ fixed while raising $\beta$, so every step to a higher coupling is also a
step to a smaller physical volume, and the charge distribution narrows with it:
at $L=32$ the exact $\chi_t V$ falls from $0.22$ at $\beta=415$ to $0.0008$ at
$\beta=1623$, where $999$ configurations in $1000$ carry $Q=0$ and there is
almost nothing left to transport. The ladder avoids this by raising $L$ and
$\beta$ together, holding the physical volume and with it $\chi_t V$ nearly
fixed from rung to rung (\sect{naive-blocking}), so $P(Q)$ at the top of the
ladder is essentially that of its base. Higher couplings at fixed physical
volume are therefore reached by climbing (\fig{ladder}), not by extending the
grid.

That caps the grid at $\beta\approx1660$, which is below the wide checkpoints'
ceiling of $2000$, so no test coupling ever asks them to extrapolate. Only the
narrow pair is tested outside its trained range, and there it fails, at all
seven couplings above \texttt{default}'s ceiling. The comparison therefore
establishes that a checkpoint fails outside its own coverage; what it cannot
establish is that widening helps for any reason beyond bringing the test back
inside coverage, since the wide checkpoints were never asked to leave it.
Separating the two would need a grid above $\beta=2000$, at $L\approx200$ to
keep the physical volume from collapsing, beyond what was run here.

The second limit is that a ceiling predicts failure only roughly, and errs both
ways: \texttt{cov60} still equilibrates at $\beta=264$, above its own ceiling of
$227$, while \texttt{default} has already failed at $\beta=174$ and $L=64$,
under half of its $416$. The volume step moves that point without harming the
lift, whose deviation is smaller at $L=64$ for all four checkpoints
(\tb{volume_scaling}). What tightens with volume is the bar the chain is held
to, not the error it makes: an ensemble mean over more sites carries a smaller
error bar, so the same bias takes longer to fall inside it.

\section{Discussion}
\label{sec:conclusions}

The results above rest on two structural properties of the two-dimensional
theories: blocking preserves $Q$ exactly, and an inexpensive update exists that
changes it. Pure 4D SU(3) has neither, and differs in four respects that bear on
the method: it is not exactly solvable, it has no trivial blocking map, it has
no trivial instanton update, and its topological charge has no unambiguous
lattice definition.

Transport needs two things, and the blocking map supplies only the first. It
pairs fine and coarse configurations while preserving $Q$ exactly, but only
because the plaquette phase is an abelian sum of link variables, so the four
fine sub-plaquettes telescope into the coarse one; an ordered product of SU(3)
matrices does not telescope. The second requirement is distributional: the
coarse ensemble's $P(Q)$ must already be the fine theory's, since an exact map
applied to a charge from the wrong distribution reproduces the wrong
distribution exactly. In 2D this is free, because the ladder holds $\chi_t V$ approximately
fixed and the deployed rungs inherit it (\sect{naive-blocking}).
In 4D the principle survives at fixed physical volume, but neither the matching
nor the charge itself stays exact: the couplings would have to be matched
numerically by scale setting \cite{sommer1994,luscher2010}, and $Q$ has no cheap
unambiguous lattice definition. The field-theoretic form requires smoothing,
whose scheme and extent measurably affect the result at finite spacing
\cite{alexandrou2015}, while the index of a Ginsparg--Wilson operator is exact
but expensive \cite{hasenfratz1998}.
Imposing $Q_{\rm coarse}$ would then be exact only with respect to a chosen
definition, and the coarse end of the ladder, where discretization artifacts are
largest, is where that choice matters most.

The instanton field is the method's other structural tool, and it serves twice:
it manufactures sector coverage in the training data, and it imposes
$Q_{\rm coarse}$ on the lifted configuration. It exists in U(2) because the
group decomposes into a U(1) factor and a topologically trivial SU(2) factor,
with the charge carried entirely by the abelian piece, so the U(1) construction
transfers directly. SU(3) does not have this property, and a cheap
topology-changing update for it is a central open question for this method. A promising method for more
complex theories is parallel tempering on boundary conditions, introduced
for the two-dimensional $\mathrm{CP}^{N-1}$ model \cite{hasenbusch2017} and
since carried to four-dimensional $SU(N)$ gauge theory and to full QCD
\cite{bonanno2021,bonanno2024}. This manufactures global topological moves for
exactly the theories that lack a cheap analytic one. This method asks less of
such a mechanism than a sampler does: it is needed only while training data is
built, and only to visit sectors, so neither its acceptance rate nor the sector
frequencies it produces need be correct. What the closed form does remain necessary for is \emph{scoring}:
every accuracy statement in this paper is a deviation from it.

Coverage also admits a route cheaper than a topological update, and one we
regard as the more promising of the two. It can be bought by deliberately
biasing the generating action rather than by moving between sectors under the
correct one:
add a term conjugate to the charge, a chemical potential $-\mu Q$ that tilts the
ensemble toward a chosen sector, or a bias potential in $Q$ that flattens the
barriers between sectors, as multicanonical sampling \cite{bergneuhaus1992} and
metadynamics do, and the latter has already been applied to lattice topological freezing
\cite{laio2016}. Ordinarily such a bias has to be undone, by reweighting each
configuration by $e^{+\mu Q}$ or by the accumulated bias potential. Here there
is nothing to undo: the biased run only populates the training set with
configurations at nonzero $Q$, and at deployment the charge is imposed by
transport from the coarse configuration rather than drawn from the model, so
the sector frequencies the biased run produces never enter any result. The model has to learn the local
structure at a given charge, which the biased ensemble supplies correctly at
each $Q$ it visits; how often it visits each one is free.

The division of labour is the part of this method we expect to outlast the
particular architecture used here. Generating UV structure and
preserving IR and topological structure are different jobs, each best done by
the tool suited to it: HMC for the UV, a learned generative model for the
topological content HMC cannot reach on its own. Normalizing flows
\cite{albergo2019}, gauge-equivariant L-CNNs \cite{favoni2020} and other
diffusion architectures \cite{zhu2024,zhu2025,aarts2026} are natural candidates
for extending it to more complicated theories.

\section*{Data and code availability}
The complete implementation, the configuration files defining every checkpoint
and ladder reported here, and the analysis scripts that produce every figure and
table are available at
\url{https://github.com/sopanda0910/inverse_rg_testing}. The summary records
behind each table are included there; generated ensembles are regenerable from
the configurations and are not distributed.

Every computation reported here, including all training runs, ladders,
validation scans and coverage retrains, was performed on a single workstation
with an AMD Ryzen 7 260 (8 cores) and one NVIDIA RTX 5060 Laptop GPU with
$8$\,GiB.

\begin{thebibliography}{99}
\bibitem{schaefer2011}
S. Schaefer, R. Sommer and F. Virotta,
\emph{Critical slowing down and error analysis in lattice QCD simulations}, Nucl. Phys. B \textbf{845} (2011) 93, [\href{https://arxiv.org/abs/1009.5228}{arXiv:1009.5228}].

\bibitem{luscher2011}
M. L\"uscher and S. Schaefer,
\emph{Lattice QCD without topology barriers}, JHEP \textbf{07} (2011) 036, [\href{https://arxiv.org/abs/1105.4749}{arXiv:1105.4749}].

\bibitem{albergo2019}
M.S. Albergo, G. Kanwar and P.E. Shanahan,
\emph{Flow-based generative models for Markov chain Monte Carlo in lattice field theory}, Phys. Rev. D \textbf{100} (2019) 034515, [\href{https://arxiv.org/abs/1904.12072}{arXiv:1904.12072}].

\bibitem{wang2024}
L. Wang, G. Aarts and K. Zhou,
\emph{Diffusion models as stochastic quantization in lattice field theory}, JHEP \textbf{05} (2024) 060, [\href{https://arxiv.org/abs/2309.17082}{arXiv:2309.17082}].

\bibitem{zhu2024}
Q. Zhu, G. Aarts, W. Wang, K. Zhou and L. Wang,
\emph{Diffusion models for lattice gauge field simulations}, [\href{https://arxiv.org/abs/2410.19602}{arXiv:2410.19602}].

\bibitem{zhu2025}
Q. Zhu, G. Aarts, W. Wang, K. Zhou and L. Wang,
\emph{Physics-conditioned diffusion models for lattice gauge theory}, JHEP \textbf{03} (2026) 111, [\href{https://arxiv.org/abs/2502.05504}{arXiv:2502.05504}].

\bibitem{aarts2026}
G. Aarts, D.E. Habibi, A. Ipp, D.I. M\"uller, T.R. Ranner, L. Wang, W. Wang and Q. Zhu,
\emph{Generalizable equivariant diffusion models for non-abelian lattice gauge theory}, [\href{https://arxiv.org/abs/2601.19552}{arXiv:2601.19552}].

\bibitem{detmoldendres2015}
W. Detmold and M.G. Endres,
\emph{Multiscale Monte Carlo equilibration: Pure Yang-Mills theory}, Phys. Rev. D \textbf{92} (2015) 114516, [\href{https://arxiv.org/abs/1510.04675}{arXiv:1510.04675}].

\bibitem{detmoldendres2016}
W. Detmold and M.G. Endres,
\emph{Multiscale Monte Carlo equilibration: Two-color QCD with two fermion flavors}, Phys. Rev. D \textbf{94} (2016) 114502, [\href{https://arxiv.org/abs/1605.09650}{arXiv:1605.09650}].

\bibitem{gattringerlang2010}
C. Gattringer and C.B. Lang,
\emph{Quantum Chromodynamics on the Lattice}, vol. 788, Springer, Berlin (2010), 10.1007/978-3-642-01850-3.

\bibitem{duane1987}
S. Duane, A.D. Kennedy, B.J. Pendleton and D. Roweth,
\emph{Hybrid Monte Carlo}, Phys. Lett. B \textbf{195} (1987) 216.

\bibitem{neal2011}
R.M. Neal,
\emph{MCMC using Hamiltonian dynamics}, in \emph{Handbook of Markov Chain Monte Carlo}, CRC Press (2011), 10.1201/b10905, [\href{https://arxiv.org/abs/1206.1901}{arXiv:1206.1901}].

\bibitem{omelyan2003}
I.P. Omelyan, I.M. Mryglod and R. Folk,
\emph{Symplectic analytically integrable decomposition algorithms: classification, derivation, and application to molecular dynamics, quantum and celestial mechanics simulations}, Comput. Phys. Commun. \textbf{151} (2003) 272.

\bibitem{albandea2021}
D. Albandea, P. Hern\'andez, A. Ramos and F. Romero-L\'opez,
\emph{Topological sampling through windings}, Eur. Phys. J. C \textbf{81} (2021) 873 [Erratum: \textbf{83} (2023) 508], [\href{https://arxiv.org/abs/2106.14234}{arXiv:2106.14234}].

\bibitem{creutz1980}
M. Creutz,
\emph{Monte Carlo study of quantized SU(2) gauge theory}, Phys. Rev. D \textbf{21} (1980) 2308.

\bibitem{kennedypendleton1985}
A.D. Kennedy and B.J. Pendleton,
\emph{Improved heat bath method for Monte Carlo calculations in lattice gauge theories}, Phys. Lett. B \textbf{156} (1985) 393.

\bibitem{amarinagaoka2000}
S. Amari and H. Nagaoka,
\emph{Methods of Information Geometry}, Translations of Mathematical Monographs \textbf{191}, American Mathematical Society, Providence (2000).

\bibitem{migdal1975}
A.A. Migdal,
\emph{Recursion equations in gauge field theories}, Sov. Phys. JETP \textbf{42} (1975) 413.

\bibitem{drouffezuber1983}
J.-M. Drouffe and J.-B. Zuber,
\emph{Strong coupling and mean field methods in lattice gauge theories}, Phys. Rept. \textbf{102} (1983) 1.

\bibitem{bonatirossi2019}
C. Bonati and P. Rossi,
\emph{Topological susceptibility of two-dimensional U(N) gauge theories}, Phys. Rev. D \textbf{99} (2019) 054503, [\href{https://arxiv.org/abs/1901.09830}{arXiv:1901.09830}].

\bibitem{anderson1982}
B.D.O. Anderson,
\emph{Reverse-time diffusion equation models}, Stochastic Process. Appl. \textbf{12} (1982) 313.

\bibitem{vincent2011}
P. Vincent,
\emph{A connection between score matching and denoising autoencoders}, Neural Comput. \textbf{23} (2011) 1661.

\bibitem{song2021}
Y. Song, J. Sohl-Dickstein, D.P. Kingma, A. Kumar, S. Ermon and B. Poole,
\emph{Score-based generative modeling through stochastic differential equations}, in \emph{International Conference on Learning Representations} (2021), [\href{https://arxiv.org/abs/2011.13456}{arXiv:2011.13456}].

\bibitem{perez2018}
E. Perez, F. Strub, H. de Vries, V. Dumoulin and A. Courville,
\emph{FiLM: Visual reasoning with a general conditioning layer}, in \emph{Proceedings of the AAAI Conference on Artificial Intelligence} \textbf{32} (2018), [\href{https://arxiv.org/abs/1709.07871}{arXiv:1709.07871}].

\bibitem{mardiajupp2000}
K.V. Mardia and P.E. Jupp,
\emph{Directional Statistics}, Wiley, Chichester (2000).

\bibitem{madrassokal1988}
N. Madras and A.D. Sokal,
\emph{The pivot algorithm: a highly efficient Monte Carlo method for the self-avoiding walk}, J. Stat. Phys. \textbf{50} (1988) 109.

\bibitem{wolff2004}
U. Wolff (ALPHA Collaboration),
\emph{Monte Carlo errors with less errors}, Comput. Phys. Commun. \textbf{156} (2004) 143 [Erratum: \textbf{176} (2007) 383], [\href{https://arxiv.org/abs/hep-lat/0306017}{arXiv:hep-lat/0306017}].

\bibitem{sommer1994}
R. Sommer,
\emph{A new way to set the energy scale in lattice gauge theories and its application to the static force and $\alpha_s$ in SU(2) Yang--Mills theory}, Nucl. Phys. B \textbf{411} (1994) 839, [\href{https://arxiv.org/abs/hep-lat/9310022}{arXiv:hep-lat/9310022}].

\bibitem{luscher2010}
M. L\"uscher,
\emph{Properties and uses of the Wilson flow in lattice QCD}, JHEP \textbf{08} (2010) 071 [Erratum: JHEP \textbf{03} (2014) 092], [\href{https://arxiv.org/abs/1006.4518}{arXiv:1006.4518}].

\bibitem{alexandrou2015}
C. Alexandrou, A. Athenodorou and K. Jansen,
\emph{Topological charge using cooling and the gradient flow}, Phys. Rev. D \textbf{92} (2015) 125014, [\href{https://arxiv.org/abs/1509.04259}{arXiv:1509.04259}].

\bibitem{hasenfratz1998}
P. Hasenfratz, V. Laliena and F. Niedermayer,
\emph{The index theorem in QCD with a finite cut-off}, Phys. Lett. B \textbf{427} (1998) 125, [\href{https://arxiv.org/abs/hep-lat/9801021}{arXiv:hep-lat/9801021}].

\bibitem{hasenbusch2017}
M. Hasenbusch,
\emph{Fighting topological freezing in the two-dimensional CP$^{N-1}$ model}, Phys. Rev. D \textbf{96} (2017) 054504, [\href{https://arxiv.org/abs/1706.04443}{arXiv:1706.04443}].

\bibitem{bonanno2021}
C. Bonanno, C. Bonati and M. D'Elia,
\emph{Large-$N$ $SU(N)$ Yang-Mills theories with milder topological freezing}, JHEP \textbf{03} (2021) 111, [\href{https://arxiv.org/abs/2012.14000}{arXiv:2012.14000}].

\bibitem{bonanno2024}
C. Bonanno, G. Clemente, M. D'Elia, L. Maio and L. Parente,
\emph{Full QCD with milder topological freezing}, JHEP \textbf{08} (2024) 236, [\href{https://arxiv.org/abs/2404.14151}{arXiv:2404.14151}].

\bibitem{bergneuhaus1992}
B.A. Berg and T. Neuhaus,
\emph{Multicanonical ensemble: a new approach to simulate first-order phase transitions}, Phys. Rev. Lett. \textbf{68} (1992) 9.

\bibitem{laio2016}
A. Laio, G. Martinelli and F. Sanfilippo,
\emph{Metadynamics surfing on topology barriers: the CP$^{N-1}$ case}, JHEP \textbf{07} (2016) 089, [\href{https://arxiv.org/abs/1508.07270}{arXiv:1508.07270}].

\bibitem{favoni2020}
M. Favoni, A. Ipp, D.I. M\"uller and D. Schuh,
\emph{Lattice gauge equivariant convolutional neural networks}, Phys. Rev. Lett. \textbf{128} (2022) 032003, [\href{https://arxiv.org/abs/2012.12901}{arXiv:2012.12901}].

\bibitem{creutz1987}
M. Creutz,
\emph{Overrelaxation and Monte Carlo simulation}, Phys. Rev. D \textbf{36} (1987) 515.

\bibitem{rusakov1990}
B.E. Rusakov,
\emph{Loop averages and partition functions in U(N) gauge theory on two-dimensional manifolds}, Mod. Phys. Lett. A \textbf{5} (1990) 693.

\bibitem{witten1991}
E. Witten,
\emph{On quantum gauge theories in two dimensions}, Commun. Math. Phys. \textbf{141} (1991) 153.

\bibitem{efron1993}
B. Efron and R.J. Tibshirani,
\emph{An Introduction to the Bootstrap}, Chapman \& Hall, New York (1993).
\end{thebibliography}

\clearpage

\appendix

\section{Network, training and sampler settings}
\label{app:settings}

The settings are as follows. The U(1) network has $324{,}954$ parameters at
hidden width $56$ and depth $4$; the U(2) one has $403{,}394$ at width $64$ and
the same depth; both use $3\times3$ kernels. Training is denoising score
matching under Adam, for $100$ epochs at batch size $16$ and learning rate
$3\times10^{-4}$ in U(1) and $120$ epochs at batch size $32$ and
$2\times10^{-4}$ in U(2), holding out a tenth of each training set for
validation; the deployed U(2) run stopped at epoch $109$ rather than $120$. The
deployed U(2) checkpoint is trained on eleven fixed $(L,\beta)$ rungs spanning
nine distinct couplings, and the deployed U(1) one on four fixed rungs plus $78$
couplings drawn at random below its ceiling; \sect{coverage} varies exactly this list and
nothing else. Sampling uses $200$ ancestral steps, each followed by one
Langevin corrector step, with the charge projection applied every ten steps
below $\sigma=0.5$. The tail is $16$ local sweeps in U(1), and in U(2) $30$
conditional SU(2) heatbath sweeps followed by $10$ local sweeps, a local sweep
being one heatbath pass over the lattice followed by two overrelaxation passes.

The training ensembles themselves are burned in once per rung, at a cost no
deployed chain could afford: $600$ to $2000$ HMC trajectories in U(1), and in
U(2) heatbath and microcanonical overrelaxation \cite{creutz1987} sweeps
followed by $300$ trajectories.

\section{Exact Observables in 2D U(1) and U(2)}
\label{app:exact}

\tb{u1-exact-observables} and \tb{u2-exact-observables} collect the closed forms
used for scoring, with the sources they follow.

\begin{table*}[t]
\centering
\small
\caption{Exact 2D U(1) observables (Wilson action). $c_0=I_0(\beta)$. Closed forms after Migdal \cite{migdal1975} and Drouffe \& Zuber \cite{drouffezuber1983}; the topological charge distribution follows Bonati \& Rossi \cite{bonatirossi2019}; the finite-$V$ Wilson loop is cf.\ Rusakov \cite{rusakov1990} and Witten \cite{witten1991}.}
\label{tab:u1-exact-observables}
\begin{tabular}{p{4.5cm}p{11.5cm}}
\toprule
Observable & Closed form \\
\midrule
Character coefficient &
  $c_q(\beta) = \dfrac{1}{2\pi}\displaystyle\int_{-\pi}^{\pi} e^{\beta\cos\theta}\,e^{-iq\theta}\,d\theta = I_q(\beta)$ \\
Mean plaquette (infinite $V$) &
  $\langle\cos\theta\rangle = r_1 = I_1(\beta)/I_0(\beta)$ \\
Wilson loop, area $A$ (finite $V$) &
  $\langle W(A)\rangle = \dfrac{\sum_q r_q^{V-A}\,r_{q+1}^{A}}{\sum_q r_q^{V}}$ \\
Wilson loop (infinite $V$) &
  $\langle W(A)\rangle \to r_1^{A}$ \\
Topological charge distribution &
  $P(Q) \propto \displaystyle\int dk\; e^{-2\pi i k Q}\,\psi(k)^{V}$,\quad
  $\psi(k) = \dfrac{\int e^{\beta\cos\theta}\cos(k\theta)\,d\theta}{\int e^{\beta\cos\theta}\,d\theta}$ \\
Topological susceptibility &
  $\chi_t = \langle Q^2\rangle / V$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[bt]
\centering
\small
\caption{Exact 2D U(2) observables. Irreps are labeled by $(j,k)$ with
$d_{(j,k)}=2j+1$, and $I_\nu$ is the modified Bessel function of order $\nu$.
After Migdal \cite{migdal1975}, with the partition function and finite-$V$
Wilson loop following Rusakov \cite{rusakov1990} and Witten \cite{witten1991},
the one-link integral Creutz \cite{creutz1980} and Kennedy \& Pendleton
\cite{kennedypendleton1985}, and $P(Q)$ Bonati \& Rossi \cite{bonatirossi2019}
applied to $w_{\rm det}$. The determinant-sector rows follow from the one-link
integral applied to the split representation; the minimum-KL matched coupling
is this work.}
\label{tab:u2-exact-observables}
\begin{tabular}{p{4.5cm}p{11.5cm}}
\toprule
Observable & Closed form \\
\midrule
Character coefficient &
  $c_{j,k}(\beta) = d_j \displaystyle\int_0^{2\pi}\dfrac{d\phi}{2\pi}\,e^{-ik\phi}\,g_j(\beta\cos\phi)$,\quad
  $g_j(a) = \dfrac{2\,I_{2j+1}(a)}{a}$ \\
Partition function &
  $Z = \displaystyle\sum_{(j,k)} \left(\dfrac{c_{j,k}}{d_{(j,k)}}\right)^{V}$ \\
Mean plaquette (infinite $V$) &
  $\langle\tfrac12\mathrm{Re\,Tr}\,P\rangle = \dfrac{I_1(x)\big(I_0(x)-I_2(x)\big)}{2\big(I_0(x)^2 - I_1(x)^2\big)}$,\ \ $x=\beta/2$ \\
Wilson loop, fundamental, area $A$ (infinite $V$) &
  $\langle\tfrac12\mathrm{Re\,Tr}\,W(A)\rangle = r_{\rm fund}^{A}$,\quad
  $r_{\rm fund} = \dfrac{c_{(1,1)}}{2\,c_{(0,0)}}$ \\
Wilson loop, fundamental (finite $V$) &
  $\langle\tfrac12\mathrm{Re\,Tr}\,W(A)\rangle = \dfrac{1}{2Z}\displaystyle\sum_{(j,k)}\ \sum_{\pm}\ \dfrac{d_j}{d_{j\pm1/2}}\left(\dfrac{c_{j,k}}{d_j}\right)^{\!A}\left(\dfrac{c_{j\pm1/2,\,k+1}}{d_{j\pm1/2}}\right)^{\!V-A}$,\ \ $d_j = 2j+1$ \\
& \footnotesize The fundamental fuses multiplicity-free, $(\tfrac12,1)\times(j,k) = (j{+}\tfrac12,k{+}1)\oplus(j{-}\tfrac12,k{+}1)$, so the double sum over $(r,r')$ collapses to two terms per irrep. Corrections to the area law are $O(e^{-\sigma(V-A)})$ from loops wrapping the torus, not a perimeter term \cite{rusakov1990,witten1991}. \\
\midrule
\multicolumn{2}{l}{\textit{Determinant sector} ($\psi=\mathrm{wrap}(2\theta)=\arg\det U$, an exact compact U(1) theory)} \\
\midrule
Single-plaquette marginal weight &
  $w_{\rm det}(\alpha) = \dfrac{2 I_1(z)}{z}$,\quad $z=\beta\cos(\alpha/2)$ \\
Topological charge distribution &
  $P(Q) \propto \displaystyle\int dk\, e^{-2\pi ikQ}\,\psi_{\rm det}(k)^V$,\quad
  $\psi_{\rm det}(k) = \dfrac{\int w_{\rm det}(\alpha)\cos(k\alpha)\,d\alpha}{\int w_{\rm det}(\alpha)\,d\alpha}$ \\
Topological susceptibility &
  $\chi_t = \langle Q^2\rangle/V$ (det.\ sector) \\
Matched U(1) coupling &
  $\beta_{\rm U(1)}(\beta)$: min.-KL projection s.t.\ $r_1^{U(1)}(\beta_{\rm U(1)}) = \langle\cos\alpha\rangle_{w_{\rm det}}(\beta)$;\ \ $\to \beta/4$ as $\beta\to\infty$ \\
\bottomrule
\end{tabular}
\end{table*}

\section{Statistical methodology}
\label{app:stats}

Standard machinery is used as specified in the sources cited where it appears,
and the two timescales are defined in \sect{relaxation-correction}. One
convention is ours.

The resampling unit is the chain. Every error bar on a sampled ensemble quantity
resamples whole chains. Beyond the usual underestimate for an autocorrelated
series \cite{wolff2004}, the case here fails outright: a topologically frozen
chain has zero within-chain variance in $Q$, so pooling over configurations
reports a vanishing error on an arm that has sampled nothing. A generated
ensemble is the exception, since a lift's configurations descend from
independent base chains and are themselves the independent unit; there the
configuration-level bootstrap and the naive standard error agree to within a few
percent, which we verify rather than assume.

\section{Error accumulation over multiple lifts}
\label{app:multilift}

Because a target may be reached by more than one lift, the error a single lift
introduces must not accumulate along the chain of them, and it does not. We
tested eight cases: both theories, an endpoint inside and one beyond the
training range, and with and without rethermalization between rungs. In every
case, three lifts to a fixed endpoint leave $0.84$--$1.02$ times the deviation
from exact that a single lift to the same endpoint leaves. The error comes
instead from the final rung, which is why endpoint accuracy tracks that rung's
distance from training coverage rather than the length of the ladder below it.

\section{Cost of building a preconditioned configuration}
\label{app:overhead}

The improvement factor of \Eq{improvement-factor} counts HMC trajectories in
both its numerator and its denominator. One cost falls outside that count:
producing the preconditioned configuration in the first place, which takes a
base ensemble at the bottom of the ladder plus one reverse diffusion per lift.
On the hardware used here that overhead is not small, and $F$ should not be
read as a wall-clock speed-up. In U(1), where the classical baseline still
converges, the one-time campaign that produced the checkpoint costs more than
every classical burn-in that converges, and the marginal cost of a lifted
configuration is larger than a trajectory's as well, so measured in seconds the
method loses at every coupling where the classical baseline is correct. In U(2)
the top rung of the ladder costs roughly twice what a winding-HMC configuration
costs, dominated by the reverse-diffusion sampler, whose step count is set for
accuracy and not tuned for speed.

What that does not do is change the comparison, for two reasons. The base
ensemble is generated once, at a coupling where sampling is cheap, and is
amortized over every configuration lifted from it; and the reverse diffusion is
a fixed number of network evaluations, the same at every coupling, while
$t_{\rm interval}$ in the numerator grows without bound as the classical chain
freezes. A fixed overhead cannot close a gap that diverges, and above
$\beta\approx90$, where no classical arm equilibrates at all, there is no finite
quantity for it to close. The case for the method is therefore the regime where
the classical baseline stops being correct at any budget, together with the
fact that the generative cost does not grow with $\beta$. It does not rest on a
wall-clock advantage where that baseline still works.

\section{Supporting tables and figures}
\label{app:tables}

This appendix collects the material referenced from the body, U(1) first and
then U(2): the classical entry cost (\tb{u1_h2h}), the freezing diagnostic
(\tb{u1_frozen}), the U(1) coverage retrain (\tb{u1_coverage_tests}), the top
rung of the ladder (\tb{u2_L128}), the per-arm U(2) benchmark
(\tb{u2_observables}), the U(2) coverage comparisons (\tb{u2_coverage_tests},
\tb{volume_scaling}), the per-configuration distributions behind
\sect{distributions} (\figs{uv_distributions}{topology_distributions}) and the
steady-state cost of the classical baselines (\tb{classical}).

\begin{table*}[htbp]
\centering
\small
\caption{U(1) coverage comparisons on the matched $L{=}8\to16$ grid. Endpoint
and columns are as in \tb{u2_coverage_tests}. All fifteen couplings are past
\texttt{deployed}'s training ceiling; ``off-rung'' is the pre-registered
subgroup of eight placed in the gaps between \texttt{wide2000}'s rungs.
Widening the range buys a factor of $37$--$50$ here, the U(1) counterpart of
the U(2) split in \sect{coverage}.}
\label{tab:u1_coverage_tests}
\begin{tabular}{llrrl}
\toprule
comparison & subset & $n$ & wins & median ratio \\
\midrule
\texttt{wide2000} vs \texttt{deployed} & all & 15 & 15 & $45.3\times$ \\
\texttt{wide2000\_dense} vs \texttt{deployed} & all & 15 & 15 & $50.4\times$ \\
\texttt{wide2000} vs \texttt{deployed} & off-rung & 8 & 8 & $37.4\times$ \\
\texttt{wide2000\_dense} vs \texttt{deployed} & off-rung & 8 & 8 & $45.2\times$ \\
\addlinespace
\texttt{wide2000\_dense} vs \texttt{wide2000} & all & 15 & 9 & $1.1\times$ \\
\texttt{wide2000\_dense} vs \texttt{wide2000} & off-rung & 8 & 4 & $1.1\times$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\caption{U(1): cold-started HMC with the winding update, $L=32$. Entry cost is
the burn-in, in trajectories, at which the quality verdict is read; quality is
whether every Wilson observable sits within $2.5$ standard errors of exact,
judged separately from $\langle Q^2\rangle$ because the winding update
samples topology correctly well before local observables equilibrate. The
$\langle Q^2\rangle$ column is the $500$-trajectory value on every row,
including the two whose entry cost is read at $8000$. The preconditioned chain's
entry cost is zero at all four couplings.}
\label{tab:u1_h2h}
\begin{tabular}{lrrrl}
\toprule
$\beta_f$ & entry cost (traj) & $\langle Q^2\rangle$ & exact $\langle Q^2\rangle$ & quality \\
\midrule
4.44   & 500          & $7.08 \pm 0.38$  & 6.79  & pass \\
14.15  & 500          & $1.87 \pm 0.058$ & 1.90  & pass \\
55.02  & 500 / 8000   & $0.47 \pm 0.008$ & 0.474 & fail at 500, pass at 8000 \\
218.58 & 500 / 8000   & $0.032 \pm 0.002$ & 0.029 & fail at 500 and at 8000 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\caption{U(1): sector changes in $3000$ trajectories from a cold start,
$L=32$. Plain periodic HMC never changes sector at any coupling tested, which is
why the freezing diagnostic is a sector-change count rather than
$\tau_{\rm int}$.}
\label{tab:u1_frozen}
\begin{tabular}{lrrr}
\toprule
$\beta_f$ & exact $\langle Q^2\rangle$ & plain HMC & HMC + winding \\
\midrule
14.15  & 1.904 & 0 & 8{,}447 \\
55.02  & 0.474 & 0 & 5{,}092 \\
218.58 & 0.029 & 0 & 358 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\caption{U(2) preconditioner benchmark at $L=128$, $\beta=1660.08$: $32$ chains,
$400$ trajectories, against an exact plaquette of $0.99879505$ and an exact
$\langle Q^2\rangle$ of $1.0012$. Plaquette columns are relative deviations from
exact in parts per million, before any trajectory and at the last record;
$\langle Q^2\rangle$ is averaged over the last half of the history and quoted as
a ratio to exact. ``$P(Q)$ cov.'' is the exact probability of the sectors an arm
visits and ``odd'' the number of odd sectors it reaches; both must be read with
$\langle Q^2\rangle$, not alone. The lift's charge interval is wide because
plain HMC cannot change $Q$, so its $32$ chains contribute $32$ frozen charges
rather than a sampled history.}
\label{tab:u2_L128}
\begin{tabular}{lrrr@{\,$\pm$\,}lrr}
\toprule
arm & plaq. $t=0$ & plaq. final & \multicolumn{2}{c}{$\langle Q^2\rangle/$exact}
& $P(Q)$ cov. & odd \\
\midrule
lift, plain HMC    & $-0.8$ & $-0.02$ & 1.155 & 0.309 & 0.941 & 3 \\
cold $+$ even-only   & $+1200$ & $+25$ & 0.786 & 0.049 & 0.507 & 0 \\
cold $+$ marginal    & $+1200$ & $+24$ & 1.249 & 0.052 & 1.000 & 4 \\
lift $+$ marginal    & $-0.8$ & $-0.14$ & 1.244 & 0.063 & 1.000 & 4 \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{u2_2d/fig62_uv_distributions.png}
  \caption{Distributions of Wilson loops over individual configurations,
  compared with the exact result, for the preconditioned chain and for cold- and
  hot-started HMC with the same plain sampler, at the hardest coupling tested in
  each theory. Each configuration contributes one value, taken from the second
  half of every chain. The horizontal axis is the deviation from the exact value
  in units of $\sigma_{\rm cfg}$, the spread of the preconditioned distribution.
  Each panel gives $\sigma_{\rm cfg}$ and the width of each classical
  distribution relative to the preconditioned one. Distributions that fall
  outside the plotted range are shown as arrows labelled with their centre, in
  units of $\sigma_{\rm cfg}$.}
  \label{fig:uv_distributions}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{u2_2d/fig63_topology_distributions.png}
  \caption{Distributions of the topological charge compared with the exact
  result, at a coupling where plain HMC cannot change the charge in either
  theory. The insets give the exact $\langle Q^2\rangle$ and each chain's
  $\langle Q^2\rangle$ as a ratio to it. The percentage is the fraction of the
  hot-start distribution that lies outside the plotted range.}
  \label{fig:topology_distributions}
\end{figure*}

\begin{table*}[htbp]
\centering
\small
\caption{U(2) coverage comparisons, paired over identical evaluation cases on a
grid of $14$ couplings at $L=32$ and $8$ at $L=64$, each run twice. The endpoint
is the raw lift's deviation from exact before any trajectory, worst case over
the plaquette, $W(2\times2)$ and $W(4\times4)$, in parts per million. ``Wins''
counts the couplings on which the first-named checkpoint is the closer of the
two; ``median ratio'' is the median of (second)/(first), so $R$ reads as ``the
first is $R$ times closer'', with a percentile bootstrap interval
\cite{efron1993}. Two qualifications on the range-against-density comparison of
\sect{coverage}: its two medians cover different subsets, so they bound the
separation rather than measure it; and on the endpoint pre-registered for the
U(1) arm, the worst-case deviation measured in standard errors, the same two
comparisons give $9.8\times$ and $17.1\times$ rather than $34.7\times$ and
$134.5\times$, with every ordering unchanged.}
\label{tab:u2_coverage_tests}
\begin{tabular}{llrrll}
\toprule
comparison & subset & $n$ & wins & median ratio & 95\% CI \\
\midrule
\texttt{wide} vs \texttt{default} & past ceiling & 14 & 14 & $34.7\times$ & $[19.4,93.3]$ \\
\texttt{wide\_dense} vs \texttt{default} & past ceiling & 14 & 14 & $134.5\times$ & $[22.9,203.5]$ \\
\texttt{wide\_dense} vs \texttt{wide} & past ceiling & 14 & 10 & $2.2\times$ & $[0.6,4.0]$ \\
\addlinespace
\texttt{wide} vs \texttt{default} & in coverage & 30 & 20 & $1.4\times$ & $[0.99,3.6]$ \\
\texttt{cov60} vs \texttt{default} & in coverage & 30 & 21 & $2.4\times$ & $[1.6,4.7]$ \\
\texttt{wide\_dense} vs \texttt{wide} & in coverage & 30 & 20 & $2.1\times$ & $[0.95,2.6]$ \\
\texttt{wide\_dense} vs \texttt{wide} & all & 44 & 30 & $2.1\times$ & $[1.2,2.7]$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\small
\caption{U(2): raw preconditioner quality by checkpoint and volume, over the
same grid ($14$ couplings $\times\,2$ rounds at $L=32$, $8\times2$ at $L=64$).
Entries are the median over couplings of the endpoint of
\tb{u2_coverage_tests}, in parts per million; lower is better, and no
checkpoint degrades with volume.}
\label{tab:volume_scaling}
\begin{tabular}{lcc}
\toprule
 & \multicolumn{2}{c}{raw lift deviation (ppm)} \\
\cmidrule(lr){2-3}
checkpoint & $L=32$ & $L=64$ \\
\midrule
\texttt{cov60}   & 8315 & 7115 \\
\texttt{default} & 8851 & 7422 \\
\texttt{wide}    & 2148 & 1812 \\
\texttt{wide\_dense} & \textbf{1272} & \textbf{702} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\small
\caption{U(2) preconditioner benchmark: relative deviation of each arm's mean
Wilson loop from the exact closed form, in parts per million, at both
benchmarked couplings, averaged over the settled half of $400$ trajectories on
$64$ chains. The last column is $\langle Q^2\rangle$ as a ratio to the exact
$1.0012$. Arms are named by starting configuration and by which winding update
of \sect{instanton_updates_classical} is enabled.}
\label{tab:u2_observables}
\begin{tabular}{lrrrr@{\hspace{1.6em}}c}
\toprule
arm & $W(1{\times}1)$ & $W(2{\times}2)$ & $W(4{\times}4)$ & $W(8{\times}8)$
& $\langle Q^2\rangle/$exact \\
\midrule
\multicolumn{6}{l}{\textit{$L=32$, $\beta=105.651$}} \\
lift, plain HMC     & $+7$ & $-9$ & $-156$ & $-5069$ & $1.14$ \\
cold, plain HMC     & $+71$ & $+421$ & $+3201$ & $+2.5\times10^{4}$ & $0.00$ \\
hot, plain HMC      & $-5.5\times10^{4}$ & $-2.3\times10^{5}$ & $-6.5\times10^{5}$ & $-9.8\times10^{5}$ & $21.2$ \\
cold $+$ even-only & $+69$ & $+197$ & $+867$ & $+4172$ & $0.82$ \\
lift $+$ even-only & $+18$ & $+29$ & $+422$ & $+381$ & $1.05$ \\
hot $+$ even-only  & $-5.5\times10^{4}$ & $-2.3\times10^{5}$ & $-6.4\times10^{5}$ & $-9.4\times10^{5}$ & $1.10$ \\
cold $+$ marginal  & $+64$ & $+149$ & $+970$ & $+6376$ & $1.01$ \\
lift $+$ marginal  & $+1$ & $+1$ & $-227$ & $-1898$ & $0.98$ \\
\midrule
\multicolumn{6}{l}{\textit{$L=64$, $\beta=416.524$}} \\
lift, plain HMC     & $-1$ & $+4$ & $+1$ & $+341$ & $1.14$ \\
cold, plain HMC     & $+51$ & $+229$ & $+1218$ & $+7644$ & $0.00$ \\
hot, plain HMC      & $-6.3\times10^{4}$ & $-2.5\times10^{5}$ & $-7.0\times10^{5}$ & $-9.9\times10^{5}$ & $87.7$ \\
cold $+$ even-only & $+57$ & $+247$ & $+1273$ & $+7115$ & $0.89$ \\
lift $+$ even-only & $+0$ & $+4$ & $+129$ & $+1263$ & $1.03$ \\
hot $+$ even-only  & $-6.2\times10^{4}$ & $-2.4\times10^{5}$ & $-7.0\times10^{5}$ & $-9.9\times10^{5}$ & $1.16$ \\
cold $+$ marginal  & $+56$ & $+233$ & $+1182$ & $+6873$ & $0.98$ \\
lift $+$ marginal  & $-1$ & $-10$ & $-60$ & $+395$ & $1.00$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[htbp]
\centering
\small
\caption{The classical baselines, one panel per theory. \emph{Left}, U(1)
steady-state cost at $L=32$ over $3000$ trajectories per arm; ``frozen'' marks
an arm whose charge never moved, for which $\tau_{\rm int}$ is not defined.
\emph{Right}, U(2) at $L=16$ from a hot start, $256$ chains and $2000$
trajectories, where flips are changes of the parity of $Q$: the even-only move's
low $\tau_{\mathrm{int}}(Q^2)$ is an artifact of never crossing parity at all,
while the marginal move's $2$--$2.7$ is the true cost of sampling that degree
of freedom. In the right panel $\tau_{\mathrm{int}}(Q^2)$ reports healthy on a chain that
is not sampling topology, which is why it is not used on its own as an
ergodicity test; in the left panel a frozen arm has no $\tau_{\mathrm{int}}$ to
report at all.}
\label{tab:classical}
\begin{minipage}[t]{0.52\linewidth}
\centering
\begin{tabular}{r l c c c}
\toprule
$\beta$ & arm & $\tau_{\mathrm{int}}(Q^2)$ & $\langle Q^2\rangle$ & exact \\
\midrule
14.1464 & plain HMC     & frozen (0/3000) & 0.0000 & 1.9040 \\
14.1464 & HMC+winding   & 2.85(44)        & 1.8705 & 1.9040 \\
55.0237 & plain HMC     & frozen          & 0.0000 & 0.4743 \\
55.0237 & HMC+winding   & 1.20(13)        & 0.4791 & 0.4743 \\
218.58  & plain HMC     & frozen          & 0.0000 & 0.0290 \\
218.58  & HMC+winding   & 1.39(15)        & 0.0296 & 0.0290 \\
\bottomrule
\end{tabular}
\end{minipage}\hfill
\begin{minipage}[t]{0.44\linewidth}
\centering
\begin{tabular}{r rc rc}
\toprule
& \multicolumn{2}{c}{even-only} & \multicolumn{2}{c}{marginal} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
$\beta$ & flips & $\tau_{\mathrm{int}}(Q^2)$ & flips & $\tau_{\mathrm{int}}(Q^2)$ \\
\midrule
14 & 4919 & 0.550 & 72522 & 2.734 \\
21 & 13   & 0.534 & 67298 & 2.365 \\
28 & 0    & 0.548 & 61403 & 1.980 \\
\bottomrule
\end{tabular}
\end{minipage}
\end{table*}

\end{document}